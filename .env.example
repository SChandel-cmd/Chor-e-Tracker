# Supabase (create project at https://supabase.com)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Production: set to your deployed URL (e.g. https://myapp.vercel.app) so login redirects back to prod, not localhost
# Leave empty for local dev (uses current origin)
# NEXT_PUBLIC_APP_URL=

# Google OAuth Configuration
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable Google+ API and Google Identity Services
# 4. Go to APIs & Services → Credentials → Create OAuth 2.0 Client ID
# 5. Add authorized JavaScript origins: http://localhost:3000 (and your production URL)
# 6. Copy the Client ID below
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Supabase Dashboard (Authentication) – required for prod login:
# - Providers → Google → Enable, add Client ID & Secret
# - URL Configuration → Site URL = YOUR PRODUCTION URL (e.g. https://myapp.vercel.app)
#   If this is localhost, Supabase will redirect users to localhost after login.
# - URL Configuration → Redirect URLs = add BOTH:
#   https://myapp.vercel.app/auth/callback
#   http://localhost:3000/auth/callback
# (Google’s “Authorized redirect URIs” are for Google→Supabase; the redirect back to your app is Supabase only.)
